<html>
  <head>
  <title>Results</title>
  </head>
  <body>
    <h1>
      Hi {{ user }}, Your score is {{ score }}.</h1>
      <br/>
      The Top Ten Scorers are:
      <table>
      {% for item in result %}
        <tr>
          <td>
            {{ item.firstName }} {{ item.lastName }}
          </td>
          <td>{{item.email}}</td>
          <td>{{item.score}}</td>
        </tr>
      {% endfor %}
    </table>
  </body>
</html>
